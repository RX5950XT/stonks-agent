[build-system]
requires = ["hatchling>=1.28,<2"]
build-backend = "hatchling.build"

[project]
name = "stonks-agent"
version = "0.2.0"
description = "可稽核、paper-only 的投資研究與交易代理核心"
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Stonks Agent contributors" }]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "alembic>=1.17.2,<2",
    "fastapi>=0.128,<1",
    "httpx>=0.28.1,<1",
    "httpcore>=1.0.9,<2",
    "jinja2>=3.1.6,<4",
    "jsonschema>=4.26,<5",
    "opentelemetry-api>=1.39,<2",
    "opentelemetry-sdk>=1.39,<2",
    "opentelemetry-exporter-otlp-proto-http>=1.39,<2",
    "psycopg[c]>=3.3.2,<4; sys_platform == 'linux'",
    "psycopg[binary]>=3.3.2,<4; sys_platform != 'linux'",
    "pydantic>=2.12.5,<3",
    "pyjwt[crypto]>=2.13,<3",
    "pyyaml>=6.0.3,<7",
    "sqlalchemy>=2.0.45,<3",
    "stonks-contracts",
    "stonks-service-auth",
    "structlog>=25.5,<26",
    "typer>=0.21,<1",
    "botocore>=1.43.50,<1.44",
    "uvicorn>=0.41,<1",
]

[project.scripts]
stonks = "stonks_agent.entrypoints.cli:app"
stonks-deploy = "stonks_agent.entrypoints.deployment:app"
stonks-gui = "stonks_agent.entrypoints.gui:app"
stonks-worker = "stonks_agent.entrypoints.worker:app"

[project.urls]
Repository = "https://github.com/RX5950XT/stonks-agent"

[dependency-groups]
dev = [
    "cyclonedx-bom>=7.3,<8",
    "hypothesis>=6.148,<7",
    "mypy>=1.19,<2",
    "pip-audit>=2.9,<3",
    "pytest>=9.0,<10",
    "pytest-asyncio>=1.3,<2",
    "pytest-cov>=7.0,<8",
    "ruff>=0.14.10,<1",
    "types-jsonschema>=4.26,<5",
    "types-pyyaml>=6.0.12.20260518",
]

[tool.hatch.build.targets.wheel]
packages = ["src/stonks_agent"]

[tool.uv]
default-groups = ["dev"]

[tool.uv.sources]
stonks-contracts = { workspace = true }
stonks-service-auth = { workspace = true }

[tool.uv.workspace]
members = ["packages/contracts", "packages/service-auth"]

[tool.pytest.ini_options]
addopts = [
    "--strict-config",
    "--strict-markers",
    "--cov=stonks_agent",
    "--cov=stonks_contracts",
    "--cov=stonks_service_auth",
    "--cov-report=term-missing",
    "--cov-report=xml",
    "--cov-fail-under=80",
]
asyncio_mode = "strict"
testpaths = ["tests", "packages/contracts/tests"]
markers = [
    "contract: wire-contract tests",
    "e2e: end-to-end workflow tests",
    "integration: tests crossing infrastructure boundaries",
    "performance: bounded performance and capacity verification tests",
    "postgres: tests requiring a real PostgreSQL server",
    "policy: repository and security policy tests",
    "unit: isolated unit tests",
]

[tool.coverage.run]
branch = true
source = ["stonks_agent", "stonks_contracts", "stonks_service_auth"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = true

[tool.ruff]
target-version = "py312"
line-length = 88
extend-exclude = [".research", ".data"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["stonks_agent", "stonks_contracts", "stonks_service_auth"]

[tool.ruff.format]
line-ending = "lf"

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "packages"]
exclude = ["^\\.research/", "^\\.data/"]
plugins = ["pydantic.mypy"]
pretty = true
show_error_codes = true
warn_unreachable = true

[tool.pydantic-mypy]
init_forbid_extra = true
init_typed = true
warn_required_dynamic_aliases = true
